{
  "name": "@xxmm/chunk-store",
  "version": "0.2.0",
  "description": "基于内容寻址的 chunk 去重存储 (Rust 后端)",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "build:rust": "cd ../../crates/chunk-store-cli && cargo build --release"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/bun": "latest"
  }
}
